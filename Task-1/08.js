dasdasdlet sentenc = 'You cannot end a sentence with because because because is a conjunction';

console.log(sentenc.indexOf('because'));






var sentence =
  "You cannot end a sentence with because because because is a conjunction";

var splitSentence = sentence.split(" ");

console.log(splitSentence.indexOf("because"));
